import 'package:flutter/material.dart';

class AppColors {
  // Light theme tokens
  static const primary = Color(0xFF2B6BF3);
  static const primaryLight = Color(0xFF5B8FF7);
  static const primarySoft = Color(0xFFE8F0FE);
  static const bg = Color(0xFFF7F8FA);
  static const surface = Color(0xFFFFFFFF);
  static const textPrimary = Color(0xFF0F172A);
  static const textSecondary = Color(0xFF64748B);
  static const border = Color(0xFFE2E8F0);
  static const success = Color(0xFF22C55E);
  static const danger = Color(0xFFEF4444);
  static const info = Color(0xFF2B6BF3);

  // Accent (charts / highlights)
  static const gold = Color(0xFFFBBF24);
  static const goldLight = Color(0xFFFDE047);

  // Dark canvas (race chart inset)
  static const navyDark = Color(0xFF0F172A);
  static const navyMedium = Color(0xFF1E293B);

  // Slate scale (secondary text / borders)
  static const slate300 = Color(0xFFCBD5E1);
  static const slate400 = Color(0xFF94A3B8);
  static const slate700 = Color(0xFF334155);
  static const slate800 = Color(0xFF1E293B);

  // Soft card tints (landing)
  static const cardTintYellow = Color(0xFFFFF8E7);
  static const cardTintLavender = Color(0xFFEEF2FF);
}
